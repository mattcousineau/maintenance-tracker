import ChatFormSkeleton from "../components/Skeleton";
//this is pretty cool - can export the component directly instead of making new one
export default ChatFormSkeleton;
/* const LoadingNewRequestPage = () => {
  return (
    <div>
      <RequestFormSkeleton />
    </div>
  );
};

export default LoadingNewRequestPage;
 */
